// Pi value in Math Object

console.log(Math.PI.toFixed(2));

// Basic Operations

// 1. Math.abs() method: Basically it is a static method that returns the absolute value of a number or in simple words we can say that how far the number is from 0. It will be alaways positive

console.log(Math.abs(4));

// 2. Math.round(x): that rounds a number to the nearest integer

console.log(Math.round(3.5));

// 3. Math.ceil(x): it returns the value of x rounded up to its nearest Integer

console.log(Math.floor(3.7));

// 4. Math.trunc(x): Basically it retuns the integer part of X

console.log(Math.trunc(3.7));



// Exponential and Logarathemic function

console.log(Math.pow(2,3)); // 2 * 2 * 2

// Math.sqrt(x): It retuns the square root of x

console.log(Math.sqrt(5));

// Math.log(x): It retuns the Natural Logarithm function

console.log(Math.log(2));

// Math.log2(X): It retuns the base2 logarithm of x

console.log(Math.log2(2));


// Generating a random number between 0 (inclusive) and 1(exclusive)

console.log(Math.random());