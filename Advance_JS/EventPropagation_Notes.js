// Basically it is a process that how events propagate or travel through the DOM (Document Object Model) hierarchy.

// Basically there are mainly 2 phases in Event propagation --> capturing phase and bubbling phase

// Basically if any event is travelling from bottom (target element) to upward(root of the DOM) then it is known as bubbling phase. And js provides it by default.

// if any event is travelling from top(root of the DOM) to bottom(target element) then it is known as capturing phase.