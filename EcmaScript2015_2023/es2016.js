// Exponential Operator

// ES7 introduces new mathematical operator that is exponential operator which is similar to Math.pow() method.
// Synatx is ( baseValue ** exponentialValue ) double asterisk. It is only used with numeric values

let baseValue = 2;
let exponentialValue = 3;
console.log(baseValue ** exponentialValue);